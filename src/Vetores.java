public class Vetores {
    public static void main(String[] args) {
    // Vetor -> uma variável também em memória, super
    // dimensionada, ou seja, ele permite o armazenamento
    // de mais de um valor ao mesmo tempo
    // O vetor possui índice [0] [1] [3] [4]
    // x[0] = 1;
    // x[1] = 10;
    // x[10] = 3;
    // O vetor é lido ou exibido sempre dentro de uma
    // repetição -> usar do FOR
    // DECLARAÇÃO: double notas[] = new double [4];
    //             int i;
    // LEITURA:    for (i=0; i<=3; i++) {
    //                System.out.println ("Digite uma nota:");
    ///               notas[i] = entrada.nextDouble(); }
    }
}
